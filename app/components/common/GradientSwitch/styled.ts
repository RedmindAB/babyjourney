import styled from 'styled-components/native'
import theme from '../../../theme'

export const GradientSwitchContainer = styled.View`
  padding: ${theme.BASELINE / 2}px;
  border-radius: 9999;
  background-color: #ffdacb;
  flex-direction: row;
  flex-wrap: wrap;
`
