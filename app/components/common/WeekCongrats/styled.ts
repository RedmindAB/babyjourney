import styled from 'styled-components/native'
import theme from '../../../theme'

export const WeekCongratsContainer = styled.View`
  width: 100%;
  align-items: center;
`

export const ConfettiContainer = styled.View`
  margin: ${theme.BASELINE * 4}px 0;
`
