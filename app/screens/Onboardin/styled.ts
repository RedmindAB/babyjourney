import styled from 'styled-components/native'
import { CenteredFill } from '../../components/common/styled'
import theme from '../../theme'

export const OnboardingContainer = styled(CenteredFill)`
  padding: ${theme.SCREEN_PADDING}px;
`
