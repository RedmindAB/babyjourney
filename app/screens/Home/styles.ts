import { StyleSheet, ViewStyle } from 'react-native'
import { variables } from '../../theme'

export default StyleSheet.create({
  container: {
    padding: 0,
    paddingTop: variables.BASELINE
  } as ViewStyle
})
